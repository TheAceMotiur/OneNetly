name = "onenetly"
main = "worker.js"
compatibility_date = "2024-01-01"

# Assets configuration for serving static files
[site]
bucket = "./dist"

# Build configuration
[build]
command = "npm run build"

# Environment variables (if needed)
# [vars]
# API_KEY = "your-api-key"

# Routes configuration (optional)
# routes = [
#   { pattern = "onenetly.com/*", zone_name = "onenetly.com" }
# ]
